import { Meta, Story, Preview, Props, Description } from '@storybook/blocks';

<Meta title="Components / Selection controls / Chip" />

# Chip (on Developing)

`Chips` can display data or be used for short action

## Usage

Chips are used to fill in data when a user gives queries, or act as a filter to interact with content. They can be exclusive or complementary.

For more details, see our [Chip guideline](https://zeroheight.com/9b39bb2a0/p/779f6f-chips/b/964f27)

## Default

<div class="mas-chip sb-unstyled">
  <div class="mas-chip_label">Default</div>
</div>

CSS styles for `Chip` component:

```css
/* Chip styles */
.chip {
  /* chip styles here */
}
.chip-label {
  /* chip-label styles here */
}
```

## Filter Chip

<div class="mas-chip mas-chip--selected sb-unstyled">
  <div class="mas-chip_left-content">
    <i class="mas-icon mas-checked" />
  </div>
  <div class="mas-chip_label">Filter Chip</div>
</div>

CSS styles :

```css
/* Chip styles */
.filter-chip {
  /* filter-chip styles here */
}
.chip-label {
  /* chip-label styles here */
}
```

## Chip with Image

<div class="mas-chip mas-chip_has-left-img sb-unstyled">
  <div class="mas-chip_left-content">
    <img class="mas-chip_image" src="/assets/img/chip-img.png" />
  </div>
  <div class="mas-chip_label">Input chip</div>
</div>

CSS styles :

```css
/* Chip styles whene has image */
.has-left-img {
  /* styles here */
}
.chip-image {
  /* styles here */
}
```

## Chip with icons

<div class="mas-chip mas-chip--has-left-icon sb-unstyled">
  <div class="mas-chip_left-content">
    <i class="mas-check-outlined mas-icon mas-system-and-device--outlined" />
  </div>
  <div class="mas-chip_label">Input chip</div>
  <div class="mas-chip_right-content">
    <i class="mas-icon mas-math-and-finance--outlined mas-x-outlined right" />
  </div>
</div>

CSS styles :

```css
/* Chip styles whene has Icons */
.has-left-icon {
  /* styles here */
}
.left-content {
  /* styles here */
}
.right-content {
  /* styles here */
}
```
