import { Meta, Source } from '@storybook/blocks';
import tailwind from '@masoni/css-tailwind-preset';
import spacing from './_spacing';
import sizing from './_sizing';

<style>
  {`
        .item {
          height: 4px;
          background-color: rgb(34 211 238);
        }
        .table-cell {
            display: table-cell;
        }
    `}
</style>

<Meta title="Guidelines / Spacings & Sizing" />

# Spacings

> Spacings creates a visual balance that makes the user interface easier to scan, and allow consistency on different layout.

For more detail, see our [Spacing's guideline](https://zeroheight.com/9b39bb2a0/p/01e423-spacings/b/505645)

<Source language="json" code={JSON.stringify(spacing, null, 2)} />

So, here is an example with associated code:

<table>
  <thead>
    <th>Token</th>
    <th>Value</th>
    <th>Pixels</th>
  </thead>
  <tbody>
    {Object.keys(spacing).map((key) => {
      if (spacing[key].value) {
        return (
          <tr>
            <td>{`--mas-core-spacing-${key}`}</td>
            <td>{spacing[key].value}px</td>
            <td class="table-cell">
              <div class="item" style={{ width: spacing[key].value + 'px' }}></div>
            </td>
          </tr>
        );
      }
    })}
  </tbody>
</table>

# Sizing

<Source language="json" code={JSON.stringify(sizing, null, 2)} />

So, here is an example with associated code:

<table>
  <thead>
    <th>Token</th>
    <th>Value</th>
    <th>Pixels</th>
  </thead>
  <tbody>
    {Object.keys(sizing).map((key) => {
      if (sizing[key].value) {
        return (
          <tr>
            <td>{`--mas-core-sizing-${key}`}</td>
            <td>{sizing[key].value}px</td>
            <td class="table-cell">
              <div class="item" style={{ width: sizing[key].value + 'px' }}></div>
            </td>
          </tr>
        );
      }
    })}
  </tbody>
</table>